We recommend installing in a clean environment. Todo so, simply run the following commands
```
conda create --name picca_mastermind python==3.10
conda activate picca_mastermind
conda install mpi4py
pip install -r requirements.txt
pip install -e .
```